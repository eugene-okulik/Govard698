# 🎭 Playwright: Шпаргалка

---

## 📦 Установка

```bash
pip install pytest-playwright
playwright install
```

```python
from playwright.sync_api import Page, expect
```

---

## ✨ Простой тест

```python
import re


def test_one(page: Page):
    page.goto('https://www.google.com')
    search_field = page.get_by_role('combobox')
    search_field.fill('cat')
    page.keyboard.press('Enter')
    expect(page).to_have_title(re.compile('^cat'))
```

---

## 🔍 Поиск элементов — Локаторы

| Метод | Описание |
|:------|:----------|
| `get_by_role()` | Поиск по роли элемента на странице (ARIA-роли) |
| `get_by_text()` | Поиск по видимому тексту |
| `get_by_label()` | Поиск по `label` искомого элемента |
| `get_by_placeholder()` | Поиск по атрибуту `placeholder` |
| `get_by_alt_text()` | Поиск по атрибуту `alt` (обычно для изображений) |
| `get_by_title()` | Поиск по атрибуту `title` |
| `get_by_test_id()` | Поиск по атрибуту `data-testid` |
| `locator()` | Поиск по CSS или XPath селектору |

---

## 🖱️ Действия с элементами

| Метод | Описание |
|:------|:----------|
| `click()` | Клик по элементу |
| `dblclick()` | Двойной клик |
| `fill()` | Ввод данных в поле ввода |
| `press_sequentially()` | Побуквенный ввод текста (эмуляция реального пользователя) |
| `check()` | Установить галочку в чекбокс |
| `press()` | Нажатие клавиши(иш) на клавиатуре |

---

## ✅ Проверка ожидаемого результата

| Метод | Описание |
|:------|:----------|
| `to_be_visible()` | Элемент виден на странице |
| `to_be_enabled()` | Элемент активен |
| `to_be_disabled()` | Элемент неактивен |
| `to_have_text()` | Точное совпадение текста в элементе |
| `to_contain_text()` | Частичное совпадение текста |
| `to_have_value()` | Поле ввода содержит указанное значение |
| `to_have_title()` | Проверка заголовка страницы |
| `to_have_url()` | Проверка URL страницы |
| `to_be_focused()` | Элемент в фокусе |
| `to_be_checked()` | Чекбокс выбран |
| `to_be_editable()` | Элемент доступен для редактирования |
| `to_be_empty()` | Контейнер пустой |
| `to_be_hidden()` | Элемент скрыт |
| `to_be_in_viewport()` | Элемент находится в видимой области страницы |
| `to_have_attribute()` | У элемента есть указанный атрибут |
| `to_have_class()` | У элемента есть указанный CSS-класс |
| `to_have_count()` | В списке указанное количество элементов |
| `to_have_css()` | У элемента есть указанное CSS-свойство |
| `to_have_id()` | У элемента есть указанный ID |
| `to_have_js_property()` | У элемента есть указанное JavaScript-свойство |
| `to_have_values()` | Элемент содержит указанные значения |
| `to_be_ok()` | Сервер вернул статус-код 200 |

---

*Создано для удобной работы с Playwright* 🚀
